// Legacy schema path retained for tooling compatibility during trx migration.

// High-performance message structure for polyglot applications
struct Msg {
  string body;      // The actual message payload
  string fromId;    // Sender ID
  string id;        // ID for idempotency
  string[] toIds;   // Array of recipient IDs
  string type;      // Type of the message
}
